jQuery(document).ready(function($) {
    $('.add-to-cart-bundle-btn').on('click', function(e) {
        e.preventDefault();
        
        // This is a placeholder. You will add AJAX code here to add all products
        // from the bundle to the cart and apply the discount.
        alert('Product bundle added to cart!');
    });
});